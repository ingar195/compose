version: '3.3'
services:
  app:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - '80:80'
      - '81:81'
      - '443:443'

    volumes:
      - /data/proxy_manager/data:/data
      - /data/proxy_manager/letsencrypt:/etc/letsencrypt


